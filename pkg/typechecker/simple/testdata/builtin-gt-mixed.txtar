-- input --
(> 10 ())

-- error --
failed to call (Callable (Int Int) Bool):
    wrong argument type for param #2 expected Int, got Unit
failed to call (Callable (Float64 Float64) Bool):
    wrong argument type for param #1 expected Float64, got Int
