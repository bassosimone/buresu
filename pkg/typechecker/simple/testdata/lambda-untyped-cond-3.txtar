-- input.txt --
(define fx (lambda (x y) (block
    (if (< x 0) (block (return! ())))
    (if (< y 10) (block (return! ())))
    (* x y)
)))
(fx 10 100)

-- expected_output.txt --
(Callable (Any Any) Any)
(Union Int Unit)
