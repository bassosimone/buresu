-- input --
(define fx (lambda (x y) (block
    (if (< x 0) (block (return! ())))
    (if (< y 10) (block (return! ())))
    (* x y)
)))
(fx 10 100)

-- output --
(Callable (Any Any) Any)
(Union Int Unit)
