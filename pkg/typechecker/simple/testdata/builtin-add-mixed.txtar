-- input.txt --
(+ 10 ())

-- expected_error.txt --
failed to call (Callable (Int Int) Int):
    wrong argument type for param #2 expected Int, got Unit
failed to call (Callable (Float64 Float64) Float64):
    wrong argument type for param #1 expected Float64, got Int
