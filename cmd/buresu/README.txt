
usage: buresu COMMAND [args...]

Buresu (ブレース, in Japanese, meaning "brace") is an intepreter for
the namesake Lisp-like, hobby programming language.

We support these commands:

    run      Runs a Buresu program.

Run `buresu COMMAND --help` for more information about `COMMAND`.
